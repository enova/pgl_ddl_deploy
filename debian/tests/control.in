# requires user "postgres", so run as root
Depends: @, postgresql-server-dev-all, postgresql-contrib-PGVERSION, postgresql-PGVERSION-pglogical
Tests: installcheck
Restrictions: allow-stderr, needs-root
